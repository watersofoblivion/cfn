(ocamllex lexer)
(menhir
 (modules parser)
 (flags
   --strict
   --explain
   --dump
   --dump-resolved
   --no-dollars))

(library
  (name syntax)
  (modes native)
  (instrumentation (backend bisect_ppx))
  (libraries
    unix
    str))
